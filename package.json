{
  "private": true,
  "name": "simple-shop",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "postinstall": "cd server && yarn && cd client && yarn",
    "start": "node server/dist/main"
  }
}
