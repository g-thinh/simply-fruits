{
  "private": true,
  "name": "simple-shop",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "postinstall": "cd server && yarn && cd clientnpm install client",
    "start": "node server/dist/main"
  }
}
